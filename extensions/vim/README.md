# pips Vim Runtime

This runtime bundle adds filetype detection and syntax highlighting for `pips` source files.

## Install

Copy or symlink these directories into your Vim runtime path:

- `ftdetect/`
- `ftplugin/`
- `syntax/`

For example, place them under `~/.vim/`.